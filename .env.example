# 백엔드 API 주소. 로컬은 Spring Boot 기본 포트다.
# 운영은 Amplify 환경변수로 주입한다 (예: https://api.example.com).
# NEXT_PUBLIC_ 접두사가 붙은 값은 브라우저 번들에 그대로 들어간다.
# 비밀·키를 여기에 넣지 않는다.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
